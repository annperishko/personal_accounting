CREATE TABLE IF NOT EXISTS users( id INT NOT NULL AUTO_INCREMENT,
                                  email text NOT NULL,
                                  account DECIMAL(10,2) NOT NULL,
                                  PRIMARY KEY (id));